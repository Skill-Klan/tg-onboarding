{
  "name": "tg_bot",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.mjs",
  "scripts": {
    "start": "NODE_ENV=production node src/index.mjs",
    "dev": "NODE_ENV=development node --watch src/index.mjs",
    "setup": "cp .env.example .env && echo '✅ .env файл створено! Відредагуй його та додай BOT_TOKEN'",
    "validate": "node -e \"import('./src/config/environment.js').then(() => console.log('✅ Конфігурація валідна'))\"",
    "check-token": "node -e \"import('./src/config/environment.js').then(({config}) => console.log('🔑 Токен:', config.bot.maskedToken))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.2.1",
    "telegraf": "^4.16.3"
  }
}
