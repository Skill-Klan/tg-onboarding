name: Deploy Bot

on:
  push:
    branches: [ main ]
    paths:
      - 'tg_bot/**'
      - '!tg_bot/pdf/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            echo "🚀 Деплой бота..."
            cd /home/roman/Стільниця/projeckt/tg-onboarding
            git pull origin main
            cd tg_bot
            npm ci --only=production
            echo "✅ Код оновлено!"
            echo "💡 Перезапустіть бота: sudo systemctl restart skillklan-bot"
