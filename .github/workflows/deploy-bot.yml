name: Deploy Bot

on:
  push:
    branches: [ main ]
    paths:
      - 'tg_bot/**'
      - '!tg_bot/pdf/**'
  workflow_dispatch:

concurrency:
  group: deploy-bot
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}       # 37.57.209.201
          username: ${{ secrets.SERVER_USER }}   # roman
          key: ${{ secrets.SERVER_SSH_KEY }}     # приватний вміст ~/.ssh/github_deploy
          port: 2222                             # ВАЖЛИВО: зовнішній порт на роутері
          timeout: 2m
          command_timeout: 15m
          script: |
            set -euo pipefail
            REPO_DIR="/home/roman/Стільниця/projeckt/tg-onboarding"
            SERVICE="skillklan-bot"

            echo "🚀 Деплой бота…"
            cd "$REPO_DIR"
            git fetch --all --prune
            git checkout main
            git pull --ff-only origin main

            cd tg_bot
            npm ci --omit=dev

            echo "🔁 Перезапуск сервісу…"
            sudo systemctl restart "$SERVICE"
            sudo systemctl is-active --quiet "$SERVICE" && echo "✅ Сервіс запущено"

            echo "ℹ️ Останні логи:"
            sudo journalctl -u "$SERVICE" -n 50 --no-pager